SELECT nazwa FROM miejsca ORDER BY nazwa ASC;
SELECT cena FROM miejsca WHERE nazwa = "Malbork";
SELECT nazwa, cena, link_obraz FROM miejsca WHERE link_obraz LIKE "0%";
SELECT miejsca.nazwa, CEIL(AVG(wycieczki.liczba_doroslych)) FROM miejsca JOIN wycieczki ON miejsca.id_miejsca = wycieczki.id_miejsca GROUP BY miejsca.nazwa;
CREATE TABLE klienci(id_klienta int PRIMARY KEY AUTO_INCREMENT not null, imie varchar(20), nazwisko varchar(40), data_ur date);
